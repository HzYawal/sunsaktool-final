service: sunsaktool-renderer
frameworkVersion: '^4'
provider:
  name: aws
  runtime: nodejs18.x
  region: ap-northeast-2
functions:
  renderVideo:
    handler: rendering-server/handler.main
    events:
      - http:
          path: /render-video
          method: post
          cors: true
