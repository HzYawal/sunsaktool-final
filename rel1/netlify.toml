[functions]
  # 백엔드 함수 코드가 들어있는 디렉토리
  directory = "netlify/functions/"
  # Node.js 런타임 버전 설정 (이대로 두시면 됩니다)
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
